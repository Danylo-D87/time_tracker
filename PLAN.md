# 📋 PLAN.md — Time Tracker: Детальний план реалізації

---

## 1. Огляд проєкту

**Мета:** Створити повноцінний веб-додаток для обліку робочого часу (Time Tracker) з чистою архітектурою, роботою зі станом, інтеграцією API та розгортанням на публічному URL.

**Ключові доменні сутності:**
- `TimeEntry` — запис часу (id, taskNameId, projectId, startTime, endTime, duration)
- `Project` — проєкт/клієнт (id, name, color)
- `TaskName` — назва задачі (id, name) — окрема таблиця для зберігання унікальних назв задач, зв'язок з TimeEntry

---

## 2. Обраний технологічний стек

| Шар                | Технологія              | Обґрунтування                                                                 |
|---------------------|-------------------------|-------------------------------------------------------------------------------|
| **Frontend**        | Next.js 14 (App Router) | SSR/SSG, API Routes, файлова маршрутизація, React Server Components           |
| **UI бібліотека**   | React 18                | Сучасний стандарт, hooks, concurrent features                                 |
| **Стилізація**      | Tailwind CSS 3          | Utility-first, швидка розробка, єдина система стилів                          |
| **UI компоненти**   | shadcn/ui               | Headless, accessible, побудований на Radix UI + Tailwind                      |
| **State management**| Zustand                 | Легковагий, простий API, без boilerplate                                      |
| **BaaS / БД**       | Supabase (PostgreSQL)   | Managed PostgreSQL, вбудована авторизація, REST/Realtime API, безкоштовний tier|
| **DB Client**       | @supabase/supabase-js   | Офіційний SDK, type-safe запити, realtime subscriptions                       |
| **ORM**             | Prisma                  | Type-safe, автогенерація типів, міграції, працює з Supabase PostgreSQL        |
| **Валідація**       | Zod                     | Runtime валідація, інтеграція з TypeScript                                    |
| **Експорт CSV**     | papaparse               | Надійний CSV парсер/генератор                                                 |
| **Дата/час**        | date-fns                | Легковагий, tree-shakeable, immutable                                         |
| **Деплой**          | Vercel                  | Нативна підтримка Next.js, безкоштовний tier                                  |
| **Мова**            | TypeScript              | Type safety, кращий DX, менше помилок                                         |

---

## 3. Архітектура проєкту

### 3.1 Принципи архітектури
- **Чітке розділення шарів:** Presentation → State/Logic → API Client → Data Layer
- **Модульність:** кожна фіча — окремий модуль
- **Заборона "все в одному файлі"** — кожен компонент, хук, сервіс у своєму файлі
- **Type-safe на всіх рівнях** — від БД до UI

### 3.2 Структура директорій

```
time_tracker/
├── prisma/
│   ├── schema.prisma              # Схема бази даних
│   ├── migrations/                # Міграції БД
│   └── seed.ts                    # Seed-дані для розробки
│
├── src/
│   ├── app/                       # Next.js App Router
│   │   ├── layout.tsx             # Кореневий layout
│   │   ├── page.tsx               # Головна сторінка (трекер)
│   │   ├── projects/
│   │   │   └── page.tsx           # Сторінка управління проєктами
│   │   ├── reports/
│   │   │   └── page.tsx           # Сторінка звітів
│   │   └── api/                   # API Routes
│   │       ├── time-entries/
│   │       │   ├── route.ts       # GET (list), POST (create)
│   │       │   ├── active/
│   │       │   │   └── route.ts   # GET — поточний активний таймер (для відновлення)
│   │       │   └── [id]/
│   │       │       └── route.ts   # GET, PUT, DELETE конкретного запису
│   │       ├── projects/
│   │       │   ├── route.ts       # GET (list), POST (create)
│   │       │   └── [id]/
│   │       │       └── route.ts   # GET, PUT, DELETE конкретного проєкту
│   │       ├── tasks/
│   │       │   └── route.ts       # GET — автодоповнення назв задач
│   │       └── reports/
│   │           └── route.ts       # GET — звіти за період
│   │
│   ├── components/                # UI компоненти (Presentation Layer)
│   │   ├── ui/                    # Базові UI компоненти (shadcn/ui)
│   │   │   ├── button.tsx
│   │   │   ├── input.tsx
│   │   │   ├── select.tsx
│   │   │   ├── dialog.tsx
│   │   │   ├── dropdown-menu.tsx
│   │   │   ├── badge.tsx
│   │   │   ├── card.tsx
│   │   │   ├── table.tsx
│   │   │   ├── calendar.tsx
│   │   │   ├── popover.tsx
│   │   │   └── color-picker.tsx
│   │   ├── layout/                # Layout компоненти
│   │   │   ├── header.tsx         # Шапка з навігацією
│   │   │   ├── sidebar.tsx        # Бічна панель (опціонально)
│   │   │   └── active-timer-bar.tsx # Поточний таймер (завжди видимий)
│   │   ├── tracker/               # Компоненти трекера
│   │   │   ├── timer-controls.tsx # Start/Stop кнопки
│   │   │   ├── task-input.tsx     # Поле назви задачі з автодоповненням
│   │   │   ├── project-select.tsx # Вибір проєкту з випадаючого списку
│   │   │   ├── time-entry-list.tsx # Список записів за день
│   │   │   ├── time-entry-item.tsx # Окремий запис
│   │   │   ├── time-entry-edit.tsx # Модалка/форма редагування запису
│   │   │   └── project-group.tsx  # Групування записів по проєктах
│   │   ├── projects/              # Компоненти управління проєктами
│   │   │   ├── project-list.tsx   # Список проєктів
│   │   │   ├── project-card.tsx   # Картка проєкту
│   │   │   └── project-form.tsx   # Форма додавання/редагування
│   │   └── reports/               # Компоненти звітів
│   │       ├── report-filters.tsx # Фільтри (день, тиждень, місяць)
│   │       ├── report-table.tsx   # Таблиця звіту
│   │       ├── report-chart.tsx   # Візуалізація (опціонально)
│   │       └── csv-export-button.tsx # Кнопка експорту CSV
│   │
│   ├── hooks/                     # Кастомні React хуки (State/Logic Layer)
│   │   ├── use-timer.ts           # Логіка таймера (start, stop, tick)
│   │   ├── use-time-entries.ts    # CRUD операції з записами часу
│   │   ├── use-projects.ts        # CRUD операції з проєктами
│   │   ├── use-task-suggestions.ts # Автодоповнення задач
│   │   └── use-reports.ts         # Логіка звітів та фільтрації
│   │
│   ├── store/                     # Zustand стор (State Layer)
│   │   ├── timer-store.ts         # Стан активного таймера
│   │   └── app-store.ts           # Глобальний стан додатку
│   │
│   ├── services/                  # Бізнес-логіка та API клієнт (Service Layer)
│   │   ├── api-client.ts          # Базовий HTTP клієнт (fetch wrapper)
│   │   ├── time-entry-service.ts  # Сервіс для роботи з записами часу
│   │   ├── project-service.ts     # Сервіс для роботи з проєктами
│   │   ├── task-service.ts        # Сервіс автодоповнення задач
│   │   ├── report-service.ts      # Сервіс для звітів
│   │   └── csv-export-service.ts  # Сервіс експорту в CSV
│   │
│   ├── lib/                       # Утиліти та допоміжні функції
│   │   ├── prisma.ts              # Prisma Client singleton
│   │   ├── supabase.ts            # Supabase Client (server + browser)
│   │   ├── utils.ts               # Загальні утиліти (cn, formatTime тощо)
│   │   ├── validators.ts          # Zod схеми валідації
│   │   └── constants.ts           # Константи додатку
│   │
│   ├── types/                     # TypeScript типи
│   │   ├── time-entry.ts          # Типи для записів часу
│   │   ├── project.ts             # Типи для проєктів
│   │   └── report.ts              # Типи для звітів
│   │
│   └── repositories/              # Data Access Layer (Repository Pattern)
│       ├── time-entry-repository.ts # Робота з TimeEntry в БД
│       ├── project-repository.ts    # Робота з Project в БД
│       └── task-repository.ts       # Робота з TaskName в БД
│
├── public/                        # Статичні файли
│   └── favicon.ico
│
├── .env                           # Змінні оточення (не в git)
├── .env.example                   # Приклад змінних оточення
├── .gitignore
├── next.config.js                 # Конфігурація Next.js
├── tailwind.config.ts             # Конфігурація Tailwind CSS
├── tsconfig.json                  # Конфігурація TypeScript
├── postcss.config.js              # PostCSS конфігурація
├── package.json
├── README.md                      # Документація
├── PLAN.md                        # Цей файл
└── PROMPTS.md                     # Лог промптів
```

---

## 4. Схема бази даних (Prisma Schema + Supabase)

**Підключення:** Prisma працює поверх Supabase PostgreSQL через `DATABASE_URL` (connection string з Supabase Dashboard → Settings → Database).

```prisma
datasource db {
  provider  = "postgresql"
  url       = env("DATABASE_URL")              // Supabase PostgreSQL connection string
  directUrl = env("DIRECT_URL")                // Supabase direct connection (для міграцій)
}

generator client {
  provider = "prisma-client-js"
}

model Project {
  id          String      @id @default(cuid())
  name        String
  color       String      @default("#3B82F6")  // HEX колір
  createdAt   DateTime    @default(now())
  updatedAt   DateTime    @updatedAt
  timeEntries TimeEntry[]
}

model TaskName {
  id          String      @id @default(cuid())
  name        String      @unique              // Унікальна назва задачі
  createdAt   DateTime    @default(now())
  updatedAt   DateTime    @updatedAt
  timeEntries TimeEntry[]                      // Зв'язок з записами часу
}

model TimeEntry {
  id         String    @id @default(cuid())
  startTime  DateTime
  endTime    DateTime?                         // null = таймер ще йде
  duration   Int?                              // тривалість у секундах
  projectId  String
  project    Project   @relation(fields: [projectId], references: [id])
  taskNameId String
  taskName   TaskName  @relation(fields: [taskNameId], references: [id])
  createdAt  DateTime  @default(now())
  updatedAt  DateTime  @updatedAt

  @@index([projectId])
  @@index([taskNameId])
  @@index([startTime])
  @@index([endTime])
  // Гарантія: лише один активний таймер (endTime IS NULL) в системі
  // Перевірка реалізується на рівні API, а не на рівні БД constraint
}
```

> **Примітка:** `TaskName` — окрема таблиця для зберігання унікальних назв задач. При створенні TimeEntry передається назва задачі, і API автоматично знаходить або створює відповідний запис TaskName (findOrCreate). Це забезпечує нормалізацію даних та зручне автодоповнення.

> **Supabase:** Таблиці створюються через Prisma міграції (`npx prisma migrate dev`), які застосовуються до Supabase PostgreSQL. Supabase Dashboard використовується для моніторингу, але структура БД контролюється Prisma.

---

## 5. API Ендпоінти

### 5.1 Time Entries

| Метод    | Шлях                         | Опис                                      |
|----------|------------------------------|-------------------------------------------|
| `GET`    | `/api/time-entries`          | Список записів (фільтр за датою)          |
| `GET`    | `/api/time-entries/active`   | **Поточний активний таймер** (endTime IS NULL) — для відновлення після reload |
| `POST`   | `/api/time-entries`          | Створити запис (старт таймера). **409 якщо вже є активний** |
| `GET`    | `/api/time-entries/[id]`     | Отримати конкретний запис                 |
| `PUT`    | `/api/time-entries/[id]`     | Оновити запис (стоп таймера, редагування) |
| `DELETE` | `/api/time-entries/[id]`     | Видалити запис                            |

### 5.2 Projects

| Метод    | Шлях                       | Опис                        |
|----------|----------------------------|-----------------------------|
| `GET`    | `/api/projects`            | Список усіх проєктів        |
| `POST`   | `/api/projects`            | Створити проєкт             |
| `PUT`    | `/api/projects/[id]`       | Оновити проєкт              |
| `DELETE` | `/api/projects/[id]`       | Видалити проєкт             |

### 5.3 Tasks (TaskName)

| Метод    | Шлях               | Опис                                         |
|----------|---------------------|----------------------------------------------|
| `GET`    | `/api/tasks?q=...`  | Пошук назв задач для автодоповнення           |
| `POST`   | `/api/tasks`        | Створити нову назву задачі                    |
| `DELETE` | `/api/tasks/[id]`   | Видалити назву задачі                         |

### 5.4 Reports

| Метод  | Шлях                                          | Опис                                 |
|--------|------------------------------------------------|--------------------------------------|
| `GET`  | `/api/reports?from=...&to=...`                 | Звіт за період (з агрегацією)        |
| `GET`  | `/api/reports/export?from=...&to=...&format=csv` | Експорт звіту у CSV                |

---

## 6. Детальний план реалізації по кроках

### Фаза 1: Ініціалізація проєкту та інфраструктура

#### Крок 1.1 — Створення Next.js проєкту
- Ініціалізація через `create-next-app` з TypeScript, Tailwind CSS, App Router
- Конфігурація `tsconfig.json` з path aliases (`@/`)
- Налаштування ESLint та Prettier

#### Крок 1.2 — Встановлення залежностей
- `prisma`, `@prisma/client` — ORM
- `@supabase/supabase-js` — Supabase клієнт
- `zustand` — state management
- `zod` — валідація
- `date-fns` — робота з датами
- `papaparse` — CSV експорт
- `shadcn/ui` — UI компоненти (init + потрібні компоненти)

#### Крок 1.3 — Налаштування Supabase + Prisma
- Створення проєкту в Supabase Dashboard (безкоштовний tier)
- Отримання `DATABASE_URL` та `DIRECT_URL` з Supabase Dashboard → Settings → Database
- Налаштування `.env` з Supabase credentials:
  - `DATABASE_URL` — pooled connection (для runtime)
  - `DIRECT_URL` — direct connection (для міграцій)
  - `NEXT_PUBLIC_SUPABASE_URL` — Supabase project URL
  - `NEXT_PUBLIC_SUPABASE_ANON_KEY` — Supabase anon key
- Створення `prisma/schema.prisma` з `provider = "postgresql"`
- Запуск першої міграції (`npx prisma migrate dev`)
- Створення Prisma Client singleton (`src/lib/prisma.ts`)
- Створення Supabase Client (`src/lib/supabase.ts`)
- Створення seed-файлу з тестовими даними

#### Крок 1.4 — Базова структура директорій
- Створення усіх директорій згідно з архітектурою
- Створення базових файлів (`utils.ts`, `constants.ts`, `validators.ts`)
- Налаштування types

---

### Фаза 2: Data Layer (Repository Pattern)

#### Крок 2.1 — Project Repository
- `findAll()` — отримати всі проєкти
- `findById(id)` — отримати проєкт за ID
- `create(data)` — створити проєкт
- `update(id, data)` — оновити проєкт
- `delete(id)` — видалити проєкт (з перевіркою на пов'язані записи)

#### Крок 2.2 — TimeEntry Repository
- `findAll(filters)` — список записів з фільтрацією за датою
- `findById(id)` — отримати запис за ID
- `findActive()` — знайти активний таймер (`WHERE endTime IS NULL`). **Повертає максимум 1 запис** — це джерело правди для відновлення таймера після перезавантаження сторінки
- `create(data)` — створити запис. **Перед створенням перевіряє `findActive()` — якщо є активний таймер, кидає помилку**
- `update(id, data)` — оновити запис
- `delete(id)` — видалити запис
- `getGroupedByProject(dateFrom, dateTo)` — агрегація по проєктах

#### Крок 2.3 — TaskName Repository
- `findAll()` — отримати всі назви задач
- `findById(id)` — отримати назву задачі за ID
- `findByName(name)` — знайти задачу за точною назвою
- `search(query)` — пошук назв задач для автодоповнення (ILIKE)
- `findOrCreate(name)` — знайти існуючу або створити нову назву задачі (використовується при створенні TimeEntry)
- `create(data)` — створити нову назву задачі
- `delete(id)` — видалити назву задачі (з перевіркою на пов'язані TimeEntry)

---

### Фаза 3: API Routes

#### Крок 3.1 — API для проєктів
- `GET /api/projects` → `projectRepository.findAll()`
- `POST /api/projects` → валідація Zod + `projectRepository.create()`
- `PUT /api/projects/[id]` → валідація + `projectRepository.update()`
- `DELETE /api/projects/[id]` → `projectRepository.delete()`
- Обробка помилок, статус-коди, edge cases

#### Крок 3.2 — API для записів часу
- `GET /api/time-entries?date=YYYY-MM-DD` → фільтрація за датою
- `GET /api/time-entries/active` → **отримати поточний активний таймер** (для відновлення після перезавантаження). Повертає запис де `endTime IS NULL` або `null`
- `POST /api/time-entries` → створення + **ОБОВ'ЯЗКОВА перевірка: якщо вже є запис з `endTime = null`, повертати 409 Conflict** з повідомленням "Таймер вже запущений для іншого завдання"
- `PUT /api/time-entries/[id]` → оновлення (стоп таймера, редагування назви/часу)
- `DELETE /api/time-entries/[id]` → видалення
- Автоматичний розрахунок `duration` при зупинці (`endTime - startTime` в секундах)

#### Крок 3.3 — API для задач (TaskName)
- `GET /api/tasks?q=...` → пошук назв задач для автодоповнення
- `POST /api/tasks` → створення нової назви задачі
- `DELETE /api/tasks/[id]` → видалення назви задачі (з перевіркою на пов'язані записи)

#### Крок 3.4 — API для звітів
- `GET /api/reports?from=...&to=...` → агреговані дані
- `GET /api/reports/export?from=...&to=...&format=csv` → генерація CSV

---

### Фаза 4: Service Layer (API Client)

#### Крок 4.1 — Базовий API клієнт
- `apiClient.get<T>(url, params)` — GET запити
- `apiClient.post<T>(url, body)` — POST запити
- `apiClient.put<T>(url, body)` — PUT запити
- `apiClient.delete(url)` — DELETE запити
- Обробка помилок, типізація відповідей

#### Крок 4.2 — Сервіс записів часу (`time-entry-service.ts`)
- `getEntries(date)` — отримати записи за дату
- `getActiveTimer()` — **отримати активний таймер з сервера** (для відновлення після reload)
- `startTimer(taskName, projectId)` — почати таймер. **Перед запуском перевіряє `getActiveTimer()` — якщо є активний, блокує старт нового**
- `stopTimer(id)` — зупинити таймер
- `updateEntry(id, data)` — оновити запис
- `deleteEntry(id)` — видалити запис

#### Крок 4.3 — Сервіс проєктів (`project-service.ts`)
- `getProjects()` — список проєктів
- `createProject(data)` — створити
- `updateProject(id, data)` — оновити
- `deleteProject(id)` — видалити

#### Крок 4.6 — Сервіс задач (`task-service.ts`)
- `searchTasks(query)` — пошук назв задач для автодоповнення
- `createTask(name)` — створити нову назву задачі
- `deleteTask(id)` — видалити назву задачі

#### Крок 4.4 — Сервіс звітів (`report-service.ts`)
- `getReport(from, to)` — отримати звіт
- `exportCSV(from, to)` — експорт CSV

#### Крок 4.5 — Сервіс CSV (`csv-export-service.ts`)
- Формування CSV з даних звіту через papaparse
- Генерація та скачування файлу

---

### Фаза 5: State Management (Zustand + Hooks)

#### Крок 5.1 — Timer Store (`timer-store.ts`)

**Ключовий принцип: таймер є server-driven.** Джерело правди — запис `TimeEntry` в Supabase з `endTime = null`. Клієнтський стор лише відображає серверний стан та оновлює `elapsed` щосекунди.

```
State:
- isRunning: boolean
- activeEntryId: string | null    // ID серверного TimeEntry
- startTime: Date | null          // startTime з серверного запису (ISO string)
- elapsed: number (секунди)       // обчислюється як (now - startTime)
- currentTaskName: string
- currentProjectId: string | null
- isLoading: boolean              // завантаження стану при ініціалізації

Actions:
- restore()    // ⭐ ГОЛОВНА ДІЯ: при mount/reload запитує GET /api/time-entries/active
               //    Якщо є активний запис → встановлює isRunning=true, startTime,
               //    обчислює elapsed = Math.floor((Date.now() - startTime) / 1000),
               //    запускає tick(). Таймер продовжує з правильного місця.
               //    Якщо немає активного → isRunning=false, скидає стан.
- start(taskName, projectId)
               // 1. Перевіряє isRunning — якщо true, блокує (один таймер!)
               // 2. POST /api/time-entries → отримує створений запис з startTime
               // 3. Зберігає activeEntryId, startTime зі серверної відповіді
               // 4. Запускає tick()
- stop()       // 1. PUT /api/time-entries/[activeEntryId] з endTime=now
               // 2. Сервер обчислює duration = endTime - startTime
               // 3. Скидає стор
- tick()       // Кожну секунду: elapsed = Math.floor((Date.now() - startTime) / 1000)
               // НЕ залежить від попереднього elapsed — завжди обчислює від startTime
- reset()      // Повний скид стору
```

**Гарантія єдиного таймера — 3 рівні захисту:**
1. **UI**: кнопка Start заблокована (disabled) коли `isRunning === true`
2. **Store**: `start()` перевіряє `isRunning` перед викликом API
3. **API (сервер)**: `POST /api/time-entries` перевіряє БД на наявність запису з `endTime IS NULL` → повертає **409 Conflict** якщо такий є

#### Крок 5.2 — Custom Hooks
- **`use-timer.ts`**:
  - При першому mount викликає `store.restore()` — відновлює таймер з сервера
  - `useEffect` з `setInterval(tick, 1000)` коли `isRunning === true`
  - `clearInterval` при `isRunning === false` або unmount
  - `elapsed` обчислюється від серверного `startTime`, не від локального стану
  - Інтеграція з API: POST при старті, PUT при стопі
- **`use-time-entries.ts`**: завантаження записів, CRUD, оптимістичні оновлення
- **`use-projects.ts`**: завантаження проєктів, CRUD
- **`use-task-suggestions.ts`**: debounced пошук з API, кеш попередніх результатів
- **`use-reports.ts`**: завантаження звітів, фільтрація по періоду

---

### Фаза 6: UI компоненти (Presentation Layer)

#### Крок 6.1 — Layout та навігація
- **`layout.tsx`** — загальний layout з header
- **`header.tsx`** — навігація між сторінками (Трекер / Проєкти / Звіти)
- **`active-timer-bar.tsx`** — *sticky* панель зверху з поточним активним таймером (завжди видима, з назвою задачі, проєктом, лічильником часу та кнопкою Stop). **Відновлюється автоматично після перезавантаження сторінки** — час розраховується від `startTime` з сервера

#### Крок 6.2 — Головна сторінка (Трекер)
- **`timer-controls.tsx`** — кнопки Start/Stop, стилізація стану (зелений/червоний)
- **`task-input.tsx`** — input з autocomplete dropdown (показує попередні задачі)
- **`project-select.tsx`** — select з кольоровими індикаторами проєктів
- **`time-entry-list.tsx`** — список записів за сьогодні
- **`time-entry-item.tsx`** — один запис: назва, проєкт (з кольором), тривалість, кнопки edit/delete
- **`time-entry-edit.tsx`** — діалогове вікно: редагування назви, проєкту, ручне введення часу у форматі `гг:хв`
- **`project-group.tsx`** — групування записів по проєктах з підрахунком загального часу

#### Крок 6.3 — Сторінка проєктів
- **`project-list.tsx`** — список усіх проєктів
- **`project-card.tsx`** — картка з назвою, кольором, кількістю записів, кнопки edit/delete
- **`project-form.tsx`** — модалка додавання/редагування: назва + color picker

#### Крок 6.4 — Сторінка звітів
- **`report-filters.tsx`** — перемикач: День / Тиждень / Місяць + date picker для вибору конкретного періоду
- **`report-table.tsx`** — таблиця з проєктами, задачами, часом
- **`report-chart.tsx`** — (опціонально) кругова/стовпчикова діаграма розподілу часу
- **`csv-export-button.tsx`** — кнопка "Експорт CSV" з індикатором завантаження

---

### Фаза 7: Обробка граничних випадків та UX

#### Крок 7.1 — Edge Cases
- **🔒 Єдиний активний таймер:** заборона запуску другого таймера на 3-х рівнях (UI disabled → Store check → API 409 Conflict)
- **🔄 Персистентність таймера при перезавантаженні:**
  - При mount хук `use-timer` викликає `GET /api/time-entries/active`
  - Якщо є активний запис → `elapsed = Math.floor((Date.now() - startTime) / 1000)`, таймер продовжує
  - Якщо вкладку закрили на 2 години і відкрили → таймер покаже 2:00:00 і продовжить
  - Loading стан поки йде запит (показуємо скелетон замість таймера)
- Валідація: назва задачі не може бути порожньою
- Валідація: проєкт має бути обраний
- Захист від видалення проєкту з активними записами (або каскадне видалення з підтвердженням)
- Ручне корегування часу: перевірка що `endTime > startTime`
- Обробка мережевих помилок (показ toast/notification)
- Пустий стан (empty state) для списків
- Обробка ситуації коли сервер недоступний при активному таймері (graceful degradation)

#### Крок 7.2 — UX покращення
- Loading скелетони при завантаженні даних
- Анімації переходів (framer-motion або CSS transitions)
- Toast-повідомлення при успішних/помилкових діях
- Responsive design (mobile-friendly)
- Клавіатурні скорочення (опціонально: Space для start/stop)
- Підтвердження перед видаленням (confirm dialog)
- Автозбереження при переході між сторінками
- Favicon з індикатором активного таймера (опціонально)

---

### Фаза 8: Деплой та документація

#### Крок 8.1 — Підготовка до деплою
- Конфігурація `next.config.js` для production
- Налаштування `.env.example` з усіма Supabase змінними:
  - `DATABASE_URL` — Supabase pooled connection string
  - `DIRECT_URL` — Supabase direct connection string
  - `NEXT_PUBLIC_SUPABASE_URL` — Supabase project URL
  - `NEXT_PUBLIC_SUPABASE_ANON_KEY` — Supabase anon key
- Запуск `npx prisma migrate deploy` для продакшн міграцій

#### Крок 8.2 — Деплой на Vercel
- Підключення GitHub репозиторію до Vercel
- Налаштування environment variables
- Запуск `prisma migrate deploy` через build script
- Верифікація роботи на публічному URL

#### Крок 8.3 — Документація
- **README.md:**
  - Опис проєкту
  - Обрані технології та обґрунтування
  - Інструкція запуску локально (`git clone` → `npm install` → `npx prisma migrate dev` → `npm run dev`)
  - Структура проєкту
  - Скріншоти інтерфейсу
  - Посилання на публічний URL
- **PROMPTS.md:** лог усіх промптів, використаних при розробці

---

## 7. Послідовність виконання (Roadmap)

```
Фаза 1: Ініціалізація           [~30 хв]
  └─ 1.1 create-next-app
  └─ 1.2 залежності
  └─ 1.3 Prisma + БД
  └─ 1.4 структура

Фаза 2: Data Layer              [~30 хв]
  └─ 2.1 Project Repository
  └─ 2.2 TimeEntry Repository
  └─ 2.3 Task Repository

Фаза 3: API Routes              [~45 хв]
  └─ 3.1 Projects API
  └─ 3.2 TimeEntries API
  └─ 3.3 Tasks API
  └─ 3.4 Reports API

Фаза 4: Service Layer           [~30 хв]
  └─ 4.1 API Client
  └─ 4.2 TimeEntry Service
  └─ 4.3 Project Service
  └─ 4.4 Report Service
  └─ 4.5 CSV Export Service

Фаза 5: State Management        [~30 хв]
  └─ 5.1 Zustand Stores
  └─ 5.2 Custom Hooks

Фаза 6: UI Components           [~2 год]
  └─ 6.1 Layout + Navigation
  └─ 6.2 Tracker Page
  └─ 6.3 Projects Page
  └─ 6.4 Reports Page

Фаза 7: Edge Cases & UX         [~45 хв]
  └─ 7.1 Validations & Error Handling
  └─ 7.2 UX Polish

Фаза 8: Deploy & Docs           [~30 хв]
  └─ 8.1 Production Config
  └─ 8.2 Vercel Deploy
  └─ 8.3 README + PROMPTS
```

**Загальний орієнтовний час: ~6 годин**

---

## 8. Ключові архітектурні рішення

### 8.1 Чому Zustand, а не Redux/Context?
- Мінімальний boilerplate — менше файлів, менше коду
- Простий API — не потрібні reducers, actions, selectors
- Підтримка middleware (persist, devtools)
- Працює поза React-деревом (можна використовувати в сервісах)

### 8.2 Чому Repository Pattern?
- Ізоляція data access від бізнес-логіки
- Легко замінити ORM або БД без зміни верхніх шарів
- Централізоване місце для складних запитів
- Тестуємість — легко мокнути репозиторій

### 8.3 Чому shadcn/ui?
- Не "чорна коробка" — код компонентів копіюється у проєкт
- Accessible з коробки (Radix UI primitives)
- Повна кастомізація через Tailwind
- Не додає зайвих залежностей до бандлу

### 8.4 Чому Supabase?
- Managed PostgreSQL — не потрібно налаштовувати сервер БД
- Єдина БД для dev і prod — немає проблем з різницею між SQLite та PostgreSQL
- Безкоштовний tier достатній для проєкту (500MB, 2 проєкти)
- Вбудований Dashboard для моніторингу та дебагу запитів
- Connection pooling з коробки (через Supavisor)
- Prisma + Supabase = type-safe запити з managed інфраструктурою
- Можливість використання Supabase Realtime в майбутньому (для multi-user)

### 8.5 Чому таймер server-driven?
- **Проблема:** Локальний таймер (Zustand/localStorage) втрачається при перезавантаженні, очищенні кешу, переході на інший пристрій
- **Рішення:** Джерело правди — запис `TimeEntry` в Supabase з `endTime = null`
- При кожному mount клієнт запитує активний таймер і обчислює `elapsed` від серверного `startTime`
- Навіть якщо вкладку закрити на годину — при поверненні таймер покаже правильний час
- Гарантія єдиного таймера забезпечується на рівні сервера (409 Conflict)

### 8.6 TaskName як окрема таблиця
- `TaskName` — окрема таблиця з унікальними назвами задач
- `TimeEntry` зберігає `taskNameId` (FK) замість текстового поля `taskName`
- Нормалізація даних — немає дублювання рядків, зручна агрегація
- При створенні TimeEntry використовується `findOrCreate` — якщо задача з такою назвою вже є, використовується існуюча
- Автодоповнення працює через пошук по таблиці `TaskName` (ILIKE)

---

## 9. Формат даних CSV експорту

```csv
Project,Task,Date,Start Time,End Time,Duration (hours)
"Project Alpha","Design UI","2026-02-13","09:00","11:30","2.50"
"Project Beta","API Development","2026-02-13","12:00","14:45","2.75"
```

---

## 10. Контрольний чек-лист фінального рішення

- [ ] Кнопки Start/Stop працюють коректно
- [ ] **Таймер зберігається після перезавантаження сторінки** (elapsed обчислюється від серверного startTime)
- [ ] **Неможливо запустити більше одного таймера одночасно** (UI + Store + API 409)
- [ ] Поле назви задачі з автодоповненням
- [ ] Вибір проєкту з dropdown
- [ ] Активний таймер завжди видимий вгорі
- [ ] Список записів за сьогодні
- [ ] Редагування назви та проєкту запису
- [ ] Ручне корегування часу (гг:хв)
- [ ] Видалення записів
- [ ] Групування по проєктах з підсумком
- [ ] Сторінка управління проєктами (CRUD)
- [ ] Колір для кожного проєкту
- [ ] Звіти за день/тиждень/місяць
- [ ] Експорт CSV
- [ ] Responsive дизайн
- [ ] Чиста архітектура (розділення шарів)
- [ ] **Supabase PostgreSQL підключена та працює**
- [ ] Деплой на публічний URL
- [ ] README з інструкціями
- [ ] Лог промптів (PROMPTS.md)
